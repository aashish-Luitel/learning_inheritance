# learning_inheritance
# Python OOP – Inheritance Practice  A simple Python program demonstrating **Object-Oriented Programming (OOP)** and **inheritance**. It includes a `Vehicle` parent class and a `Car` child class that inherits its attributes and methods while adding its own `drive()` method.


